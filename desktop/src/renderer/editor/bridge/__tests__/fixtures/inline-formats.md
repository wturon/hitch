Plain text with **bold**, *italic*, ~~strikethrough~~, and `inline code`.

A run that is ***bold and italic together*** in one span.

Mixing **bold with *nested italic* inside** a sentence.

- **Bold item** in a list
- Item with *italic* and `code`
