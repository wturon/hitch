# Dependencies
node_modules/

# Build output
dist/
dist-daemon/
build/
release/
.next/
out/

# Convex

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Hitch's own synced folder (the daemon manages this; never commit it)
.hitch/

# e2e screenshot output (never commit)
desktop/e2e/shots/

# impeccable (design skill) — local/ephemeral state (docs + design.json ARE tracked)
.impeccable/config.local.json
.impeccable/critique/
**/.impeccable/live/
backups/
